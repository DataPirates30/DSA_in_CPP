#include<iostream>
using namespace std;

int main(){

    // s is between 1 and something big
    // Tom goes first, it means jerry goes second
    // So every person removes a palindrome number of stones
    // If the pile is empty on the starting it loses
    // stone constratint 100

    

    return 0;
}